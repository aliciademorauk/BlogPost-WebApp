<h1>Add New Blog Post</h1>

<p class="italic">&#x1F4A1; Not sure what to write about? Write a title idea below and let <strong>AI</strong> get you started:</p>

<%= form_tag suggest_content_path, method: :post, remote: true do %>
  <div class="flex items-center space-x-4">
    <%= text_field_tag :title, params[:title], class: "block w-full p-2 border rounded-md" %>
    <%= submit_tag "Surprise me!", class: "bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 hover:from-pink-500 hover:to-yellow-500 text-white p-2 rounded-md", id: "suggest-content-btn" %>
  </div>
<% end %>


<%= render partial: "open_ai/suggest_content", locals: { response_content: @response_content } %>
<p class="italic">Or else fill in blog post details below...</p>

<%= render partial: "form", locals: { blog_post: @blog_post } %>